// Public privacy policy at /legal/privacy. Prerendered to plain static HTML that reads
// with no JavaScript, matching /trust. The app root defaults to SPA (ssr=false); this
// route opts back into server rendering at build time. Pure content — no data, no state.
export const prerender = true;
export const ssr = true;
export const csr = false;
